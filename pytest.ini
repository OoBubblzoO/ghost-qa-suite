[pytest]
testpaths = tests
markers =
    smoke: fast, read-only checks that must pass on every commit
    critical: auth + core CRUD; the "is the product usable" set
    regression: negative tests, validation, edge cases
    e2e: Playwright browser tests (critical user journeys only)
addopts = -ra --strict-markers
junit_family = xunit2
